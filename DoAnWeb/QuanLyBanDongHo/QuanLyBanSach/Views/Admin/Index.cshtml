@*@model IEnumerable<QuanLyBanSach.Models.DONGHO>*@
@using QuanLyBanSach.Models;
@using PagedList.Mvc;
@model PagedList.PagedList<QuanLyBanSach.Models.DONGHO>
@helper Truncate (String input,int length)
{
    if(input.Length <length){
        @input
    }
    else{
        @input.Substring(0,length)<text>..........</text>
    }
}
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Admin/shared/_layout.cshtml";
}

<h2>QUẢN LÍ THÔNG TIN ĐỒNG HỒ</h2>
<a class="btn btn-primary btn-block btn-login" href="/Admin/Create">Create</a>
<p>
   @* @Html.ActionLink("Create New", "Create")*@
</p>

@*  <table class="table" border="1">
    <tr>
        <th style="width:200px">Tên đồng hồ</th>
        <th style="width:100px">Giá bán</th>
        <th style="width:300px">Mô tả</th>
        <th style="width:150px">Ngày cập nhật</th>
        <th style="width:120px">Ảnh bìa</th>
        <th style="width:120px">Số lượng tồn</th>
        <th style="width:120px">Mã hãng</th>
        <th style="width:120px">Xuất xứ</th>
        <th style="width:120px">Mới</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaBan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MoTa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayCapNhat)
        </td>
        <td>
            <img src="@Url.Content("~/HinhAnhSP/" + item.AnhBia)" style="width:50px;height:50px"/>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoLuongTon)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaHA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.XuatXu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Moi)
        </td>
        <td>
           @Html.ActionLink("Sửa", "SuaDongho", new { id=item.MaDH }) |
            @Html.ActionLink("Chi tiết", "Chitietdongho", new { id=item.MaDH }) |
            @Html.ActionLink("Xóa", "Xoadongho", new { id=item.MaDH })
        </td>
    </tr>
}

</table>
Trang@(Model.PageCount<Model.PageNumber?0:Model.PageNumber)/@Model.PageCount;
<div class="MenuTrang">
    @Html.PagedListPager(Model,page=>Url.Action("Index",new{page=page}))
</div>
<style>
    .MenuTrang li {
        display: inline;
    }
</style>*@


<div class="container wow bounceInUp">
	<table id="cart" class="table table-hover table-condensed">
    				<thead>
						<tr class ="thead-dark">
				
        <th style="width:10%">Tên đồng hồ</th>
        <th style="width:10%">Giá bán</th>
        <th style="width:20%">Mô tả</th>
        <th style="width:10%">Ngày cập nhật</th>
        <th style="width:10%">Ảnh bìa</th>
        <th style="width:5%">Số lượng tồn</th>
        <th style="width:1%">Mã hãng</th>
        <th style="width:5%">Xuất xứ</th>
        <th style="width:3%">Mới</th>
        <th style="width:27%" class="text-center" >Chức Năng</th>
						</tr>
					</thead>

         @foreach (var item in Model)
    {
					<tbody>
						<tr>
							
							<td>@item.TenDH</td>
                            <td>@item.GiaBan</td>
                     
                            <td>@Truncate(item.MoTa,50)</td>
                            <td>@item.NgayCapNhat</td>
                             <td><img src="~/HinhAnhSP/@item.AnhBia" style="width:150px;height:150px"/></td>
                        
                            <td>@item.SoLuongTon</td>
                             <td>@item.MaHA</td>
                             <td>@item.XuatXu</td>
                             <td>@item.Moi</td>
							<td class="actions" data-th="">
								 @Html.ActionLink("Sửa", "Edit", new { ma=item.MaDH }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.MaDH }) |
            @Html.ActionLink("Xóa", "Delete", new { ma=item.MaDH })
							</td>
                           
						</tr>
					</tbody>
         }
				
				</table>
</div>
Trang@(Model.PageCount<Model.PageNumber?0:Model.PageNumber)/@Model.PageCount;
<div class="MenuTrang">
    @Html.PagedListPager(Model,page=>Url.Action("Index",new{page=page}))
</div>
<style>
    .MenuTrang li {
        display: inline;
    }
</style>
